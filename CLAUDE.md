最初から無作為にファイルを閲覧することを禁じます。
ユーザーからの質問だったりに対して必要なファイルを最小限で閲覧するようにしてください。

## AI-DLC の運用(aidlc-docs/ がある場合)

- セッション再開時は `aidlc-docs/aidlc-state.md` を読み、続きから始める
- 判断・承認・質問への回答を行うたび、`aidlc-docs/audit.md` に ISO 8601 で 1 行追記する
- 成果物は対応フェーズのディレクトリへ置く。やり直すとき旧分は `<名前>.backup` に退避してから(旧仕様の混入防止)
- フェーズの境では、必要な成果物が揃ったか点検してから次へ進む
